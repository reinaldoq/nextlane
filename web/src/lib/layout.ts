/** Shared page-content max width -- Mission Control and Inventory both center
 * their content column at this width so the two top-level pages feel
 * consistent. */
export const PAGE_MAX_WIDTH = 1400
